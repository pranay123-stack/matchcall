# Rust / Anchor
/target
**/*.rs.bk
.anchor
test-ledger

# Node
node_modules
.next
out
dist
*.tsbuildinfo
npm-debug.log*

# Env & secrets
.env
.env.local
.env.*.local
app/.env.local
/.keys/*.json
!/.keys/.gitkeep

# DB
*.db
*.db-journal
*.sqlite

# OS
.DS_Store
*.log

# Keep the program + mint keypairs OUT of git (addresses are committed in docs)
target/deploy/*.json

# SQLite sidecar files
*.db-shm
*.db-wal
